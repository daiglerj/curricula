const initialState = {
    baseURL:"http://localhost:8080/"
}

const appReducer = (state=initialState,action)=>{
    switch(action.type){
        default:{
            return state
        }
    }
}
export default appReducer